extends ../externals/clientbase

block content
  include ../externals/cards

  main
    .product-title
      h2 #{serie.title || ''} - season #{season.number || ''}
      .product-tile-tags
        span= new Date(serie.releasedDate).getFullYear()
        | &nbsp; &nbsp; 
        span #{serie.rating || 'unrated'}/10
    
    .product-image
      img(src=`${serie.landscape}`)
    .product-description
      .product-tags
        .product-tags__cover
          span.tag
            a(href=`/series?text=${serie.company.split(' ').join('-')}`)= serie.company
          each tag in serie.tags
            span.tag
              a(href=`/series?text=${tag.toLowerCase().split(' ').join('-')}`)= tag
      
      p= serie.description
        
      
    .furthur-info
      .furthur-info__box
        span.furthur-info__title writers/producers/directors:
        span.furthur-info__list
          each director in serie.directors
            a(href=`/series?directors=${director.toLowerCase().split(' ')[0]}`) #{director},      
      
      .furthur-info__box
        span.furthur-info__title actors:
        span.furthur-info__list
          each actor in serie.actors
            a(href=`/series?charactors=${actor.toLowerCase().split(' ')[0]}`) #{actor},
      
    .season-links
      .season-links__cover
        each episode in season.Episodes
          +episodeCard(episode)
        //- - let a = 1
        //- while a < 5
        //-   - a++

    section.report.center-components
      button.btn-red.suggest.margin-a1 suggest
      button.btn-red.report.margin-a1 report problem

  section.container
    .content.content-movies#suggestions(data-in=`${['keywords', ...serie.keywords].join(',')}` data-not-in=`${['id',serie.id]}` data-text=`${serie.company.split(' ').join('-')}-${serie.keywords.join('-')}`)
      - let b = 1
        while b < 13
          +movieCard(b)
          - b++
    //   
    .center-components
      a.show-more(href='')
        button.btn-primary--alt.btn-small
          span.btn-content load more


  //- .popup.display-off#suggest-popup
  //-   //   
  //-   .popup-box.popup-box-normal
  //-     .popup-close
  //-       i.fas.fa-times
  //-     .popup-content.create-content
  //-       form.create-new#create-suggest
  //-         .form-heading
  //-           h1 Suggest
  //-         .form-group
  //-           label.label-basic(for="suggest-country") suggest on
  //-           select#suggest-country.input-basic(type="text")
  //-             option(value="movie") movie
  //-             option(value="serie") serie
  //-             option(value="game") game
  //-         .form-group
  //-           label.label-basic(for="suggest-activity") message
  //-           textarea#suggest-activity.input-basic(type="text")
  //-         .form-group-double
  //-           .form-group
  //-             label.label-basic(for="suggest-name") name
  //-             input#suggest-name.input-basic(type="text", placeholder="optional")
  //-           .form-group
  //-             label.label-basic(for="suggest-email") email
  //-             input#suggest-email.input-basic(type="email", placeholder="optional")
  //-         .center-components
  //-           button.btn-black.btn-normal(type="submit")
  //-             span.btn-content create


  //- .popup.display-off#problem-popup
    //   
    .popup-box.popup-box-normal
      .popup-close
        i.fas.fa-times
      .popup-content.create-content
        form.create-new#create-problem
          .form-heading
            h1 Report Problem
          .form-group
            label.label-basic(for="problem-country") problem on
            select#problem-country.input-basic(type="text")
              option(value="broken-link") broken link
              option(value="other") other
          .form-group
            label.label-basic(for="problem-activity") message
            textarea#problem-activity.input-basic(type="text")
          .form-group-double
            .form-group
              label.label-basic(for="problem-name") name
              input#problem-name.input-basic(type="text", placeholder="optional")
            .form-group
              label.label-basic(for="problem-email") email
              input#problem-email.input-basic(type="email", placeholder="optional")
          .center-components
            button.btn-black.btn-normal(type="submit")
              span.btn-content create